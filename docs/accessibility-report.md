# Accessibility report — V2.0

Сохранены semantic sections/nav, aria-live toast root, aria labels для icon controls, keyboard-focus styles и `prefers-reduced-motion` из исходной версии. Новые provenance nodes реализованы кнопками, backup controls — нативными buttons/file input, AI policy — checkbox labels.

Production gate: ручная проверка WCAG AA contrast для обеих тем, keyboard-only полного сценария, focus order и screen-reader labels после интеграции server mode.

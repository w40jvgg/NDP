/*
 * NDP V2.0 runtime switch.
 * GitHub Pages / standalone demo: keep mode = 'demo'.
 * Server deployment: set mode = 'server' and apiBaseUrl to your API origin.
 */
window.NDP_RUNTIME = window.NDP_RUNTIME || {
  mode: 'demo',
  apiBaseUrl: ''
};
